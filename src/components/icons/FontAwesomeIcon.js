import React from 'react';
import Icon from 'react-native-vector-icons/dist/FontAwesome';

const FontAwesomeIcon = (props) => <Icon type="font-awesome" {...props} />;

export default FontAwesomeIcon;
